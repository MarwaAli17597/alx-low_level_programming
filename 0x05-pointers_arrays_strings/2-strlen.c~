#include "main.h"
/**
 * _strlen - Returns the length of a string
 * @s: The string to get the length of
 *
 * Returns: The length of string
 */
int _strlen(char *s)
{
size_t length = 0;
while (*s++)
length++;
return (length);
}
